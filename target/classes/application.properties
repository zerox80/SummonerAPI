# Riot API Configuration
riot.api.key=YOUR_API_KEY
riot.api.region=euw1

# Server Port
server.port=8080

# Logging Configuration
logging.level.com.zerox80.riotapi=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE

# Thymeleaf Cache Configuration (deaktiviert für Entwicklung)
spring.thymeleaf.cache=false

# Spring Cache Configuration (using Caffeine)
spring.cache.type=caffeine
# Die Cache-Namen sollten hier nicht erneut deklariert werden, wenn sie bereits in CacheConfig.java definiert sind.
# Spring Boot sollte die in CacheConfig registrierten Caches automatisch erkennen.
# Falls du spezifische Einstellungen pro Cache hier vornehmen willst, ist das eine andere Sache.
# Fürs Erste kommentiere ich das aus, um Doppeldefinitionen zu vermeiden, wenn CacheConfig aktiv ist.
# spring.cache.cache-names=accounts,summoners,leagueEntries,matchIds,matchDetails

# H2 Database Configuration - AUSKOMMENTIERT/ENTFERNT, da PostgreSQL jetzt Standard ist
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console
# spring.datasource.url=jdbc:h2:file:./database/leagueapidb;DB_CLOSE_DELAY=-1;AUTO_SERVER=TRUE
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.hibernate.ddl-auto=update 
# spring.jpa.show-sql=true 

# ===============================
# POSTGRESQL DATASOURCE CONFIGURATION (Primäre Konfiguration)
# ===============================
spring.datasource.url=jdbc:postgresql://localhost:5432/DEINE_DATENBANK
spring.datasource.username=DEIN_BENUTZERNAME
spring.datasource.password=DEIN_PASSWORT
spring.datasource.driver-class-name=org.postgresql.Driver # Korrekter Property-Name

# ===============================
# POSTGRESQL JPA/HIBERNATE CONFIGURATION (Primäre Konfiguration)
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false

# Der untere Block "CACHING (Beispiel)" ist redundant, da spring.cache.type oben schon gesetzt ist.
# Ich entferne ihn.
